package com.example.nlp.domain;

/**
 * Representa as possíveis classes de sentimento que o sistema reconhece.
 */

public enum SentimentLabel {

    POSITIVO,
    NEGATIVO,
    NEUTRO
}
